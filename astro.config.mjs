import { defineConfig } from 'astro/config';

export default defineConfig({
  // Uncomment and update before deploying to GitHub Pages:
  // site: 'https://your-username.github.io',
  // base: '/wl_beskidzki',
});
